package com.example.parkingmanagement.mapper;

import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface SlotMapper {

//    Slot map(SlotDto slotDto);
//
//    SpotDto map(Slot slot);
}
